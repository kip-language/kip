(library
 (name kip_parse)
 (public_name kip.parse)
 (modules :standard)
 (libraries kip.shared kip.diagnostics kip.ast)
 (wrapped true))

; Uncomment when using menhir
; (menhir
;  (modules parser)
;  (flags --explain))
